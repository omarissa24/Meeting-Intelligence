# Used by docker-compose.yml for the Postgres container.
# The backend service reads its own credentials from backend/.env.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=meeting_intelligence
