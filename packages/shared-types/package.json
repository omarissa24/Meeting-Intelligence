{
  "name": "@meeting-intelligence/shared-types",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'shared-types is source-only; no build step' && exit 0",
    "typecheck": "tsc -b --noEmit",
    "lint": "echo 'no lint configured yet' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
