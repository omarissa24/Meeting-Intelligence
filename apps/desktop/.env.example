# URL of the Meeting Intelligence backend the desktop will connect to.
# Local dev: http://localhost:8000. The desktop converts http→ws when
# opening the /transcript/ws/{sessionId} socket; supply the http(s) URL,
# not the ws(s) URL.
VITE_BACKEND_URL=http://localhost:8000
