//! Windows meeting-detection signal source. Built only for Windows.
//!
//! Code-complete behind the `DetectionSource` trait but pending real-hardware
//! UAT — same staging as the WASAPI system source (`audio::windows::system`).

pub mod source;
